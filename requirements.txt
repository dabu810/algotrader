anthropic>=0.40.0
python-dotenv>=1.0.0
rich>=13.0.0
twilio>=9.0.0
flask>=3.0.0
streamlit>=1.36.0
gunicorn>=22.0.0

# Multi-model providers (install whichever you use)
openai>=1.0.0
google-genai>=0.5.0
mistralai>=1.0.0

# Web search/fetch for non-Anthropic providers
duckduckgo-search>=6.0.0
httpx>=0.27.0

# TradingView Technical Analysis Agent
mcp>=1.0.0                    # MCP Python SDK (client for MCP servers)
mcp-tradingview-server>=0.1.0 # TradingView MCP server (bidouilles)
tradingview-scraper>=0.3.0    # Fallback if MCP server is unavailable
