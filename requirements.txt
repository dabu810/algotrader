anthropic>=0.40.0
python-dotenv>=1.0.0
rich>=13.0.0
twilio>=9.0.0
flask>=3.0.0
streamlit>=1.36.0
gunicorn>=22.0.0

# Multi-model providers (install whichever you use)
openai>=1.0.0
google-genai>=0.5.0
mistralai>=1.0.0

# Web search/fetch for non-Anthropic providers
duckduckgo-search>=6.0.0
httpx>=0.27.0

# TradingView Technical Analysis Agent
# mcp and mcp-tradingview-server are optional — only needed if the MCP binary is installed
# Install manually: pip install mcp && pip install git+https://github.com/bidouilles/mcp-tradingview-server.git
tradingview_ta>=0.3.10        # TradingView scanner API (primary data source)
yfinance>=0.2.40              # Historical OHLCV data fallback
